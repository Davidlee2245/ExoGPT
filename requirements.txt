# Core ExoGPT dependencies
# Python 3.10 required
# EXACT VERSIONS - DO NOT CHANGE WITHOUT TESTING
numpy==1.26.4  # PyTorch 2.1.0 requires numpy < 2.0, MUST be 1.26.4
scipy==1.11.4  # MUST be < 1.12 for numpy 1.26.4 compatibility
pandas==2.3.3
biopython==1.86
pyyaml==6.0.3
flask==3.1.2
flask-cors==6.0.1
markupsafe==3.0.3  # Required by Flask/Werkzeug
jinja2==3.1.6  # Required by Flask
requests==2.32.5
beautifulsoup4>=4.12.0
openai>=1.0.0
openpyxl>=3.1.0
PyPDF2>=3.0.0
python-docx>=1.1.0

# mRNA biomarker discovery and primer design dependencies
# Note: Primer3 must be installed separately via conda:
# conda install -c bioconda primer3
# This is required for step2_primer_design.py

# PyTorch with CUDA 11.8 (install separately via pip)
# Note: PyTorch 2.1.0 with cu118 works with CUDA 11.6 toolkit (backward compatible)
# pip install torch==2.1.0 torchvision==0.16.0 torchaudio==2.1.0 --index-url https://download.pytorch.org/whl/cu118
# conda install cudatoolkit=11.6 -c conda-forge -y

# RFdiffusion dependencies
# Note: RFdiffusion itself should be installed with: cd RFdiffusion && pip install -e . --no-deps
# SE3Transformer: cd RFdiffusion/env/SE3Transformer && pip install -e .
omegaconf==2.3.0
hydra-core==1.3.2
e3nn==0.5.8
wandb==0.23.0
# DGL with CUDA 11.6 support - MUST install via conda (NOT pip):
# conda install -c dglteam dgl-cuda11.6
# This installs dgl==0.9.1.post1 with CUDA 11.6 GPU support
# DO NOT use pip install dgl - that installs CPU-only version!
pyrsistent==0.20.0  # Required by RFdiffusion

